# Explanation

Large files are placed in gitlab and synchronized to github in the form of sub-repositories

```sh
$ git clone https://github.com/khadas/aml_npu_demo_binaries.git
$ cd aml_npu_demo_binaries
$ git lfs pull
```
